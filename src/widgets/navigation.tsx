import { Logo } from 'shared/ui';

export const Navigation = () => {
    return (
        <nav className="flex justify-center py-4 border-b border-[#233251]">
            <Logo />
        </nav>
    );
};
